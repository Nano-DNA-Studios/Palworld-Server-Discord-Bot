
/**
 * Enum for all the Bash Scripts
 */
enum BashScriptsEnum {
    Start = 'start',
    Restart = 'restart',
    Shutdown = 'shutdown',
    Backup = 'backup',
    Update = 'update',
    Custom = 'custom',
    Ping = 'ping',
}

export = BashScriptsEnum;
